apply plugin: "war"

bootWar {
    mainClassName = "com.fleta.purchasingservice.PurchasingServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
